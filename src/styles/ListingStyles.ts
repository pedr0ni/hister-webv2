import { makeStyles } from "@material-ui/core";

export const useListingStyles = makeStyles({
    table: {
        minWidth: 650,
    },
    tableContainer: {
        marginTop: 20
    },
    button: {
        marginTop: 10,
        marginBottom: 20
    }
});
